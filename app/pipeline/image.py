from app.models import ContentPack


async def generate_logo(pack: ContentPack) -> bytes | None:
    """Implemented in T4. Never raises — returns None on any failure or at the cap."""
    raise NotImplementedError
